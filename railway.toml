[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/snapshots"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"

[environments.production.variables]
DB_PATH = "/data/snapshots.db"
